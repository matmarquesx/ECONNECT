{
  "compilerOptions": {
    "target": "es2016",
    "types": ["jest"],                          /* Define a versão da linguagem JavaScript. */
    "module": "commonjs",                        /* Define o tipo de módulo. */
    "jsx": "react-jsx",                          /* Habilita o suporte a JSX para React (React 17+). */
    "esModuleInterop": true,                     /* Garante compatibilidade com módulos CommonJS. */
    "strict": true,                              /* Habilita verificações de tipo estritas. */
    "forceConsistentCasingInFileNames": true,    /* Garante que nomes de arquivos sejam sensíveis a maiúsculas e minúsculas. */
    "skipLibCheck": true                         /* Ignora a verificação de tipos em arquivos .d.ts. */
  }
}
